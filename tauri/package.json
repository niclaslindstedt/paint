{
  "name": "paint-tauri",
  "version": "0.1.0",
  "author": "Niclas Lindstedt <niclas@agilator.se>",
  "description": "Desktop wrapper around the Paint PWA — bundles the app on-device and serves it from a private scheme, in the platform's own webview. See README.md.",
  "private": true,
  "scripts": {
    "icons": "node ../scripts/generate-icons.mjs",
    "bundle": "node scripts/bundle-web.mjs",
    "bundle:copy": "node scripts/bundle-web.mjs --skip-build",
    "build": "cargo build",
    "start": "npm run bundle && cargo run --bin paint",
    "start:fast": "npm run bundle:copy && cargo run --bin paint",
    "test": "cargo test -p paint-shell",
    "lint": "cargo clippy --workspace --all-targets -- -D warnings",
    "fmt": "cargo fmt --all",
    "fmt:check": "cargo fmt --all -- --check",
    "package": "npm run bundle && tauri build",
    "package:debug": "npm run bundle && tauri build --debug"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
