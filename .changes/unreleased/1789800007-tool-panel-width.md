---
type: Fixed
doc: plugins
---

The panel behind a tool button is now only as wide as the family it holds, so the fills and the erasers open two buttons wide instead of in a four-wide box with half of it empty.
