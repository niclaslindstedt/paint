---
type: Changed
title: Type sizes shown at their real size
doc: plugins
---

The text tool's size button and its size row now draw the sample letter at the
size it will actually land at — clipped by the button when it is bigger than the
button — instead of shrinking every size to the same letter.
