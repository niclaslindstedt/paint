---
type: Changed
title: Dropped pictures sync as real files
doc: cloud-sync
---

A picture you drop on the page is now filed beside the synced document as a genuine `.png` / `.jpg` under `images/`, instead of riding along inside the JSON — so the document stays small, and the pictures are browsable in Dropbox, Google Drive, or the folder you picked.
